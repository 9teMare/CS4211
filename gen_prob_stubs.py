#!/usr/bin/env python3

# These are stubs to test whether the pipeline works
# Eventually, both gen_prob will actually have to generate probability based on
# model path instead of the name

def gen_prob(model_file_path):
  return 0.5

def gen_softmax_home_prob(home_prob, away_prob):
  return 0.5
